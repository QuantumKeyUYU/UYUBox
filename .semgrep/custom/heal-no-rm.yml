rules:
  - id: no-rm-in-heal
    message: Avoid destructive rm -rf calls in self-heal modules
    severity: ERROR
    languages: [python]
    pattern: subprocess.$FUNC(["rm", "-rf", ...])
    paths:
      include: ["src/uyubox_core/self_heal/**"]
